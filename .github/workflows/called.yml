name: called workflow
on:

 workflow_call:
  inputs:
      branch:
        required: true
        type: string
jobs: 
 
  build:
     runs-on: ubuntu-latest
   
    
     steps:
         - name: step-1
           run: |
              echo $hostname
              echo $UID
         - name: step-2
           run: |
               echo "trying to be called workflow" 
